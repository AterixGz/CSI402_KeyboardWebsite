@{
    ViewData["Title"] = "Customers";
    Layout = "_AdminLayout";
}

<div class="page">

    <!-- ── Top bar ── -->
    <div class="topbar">
        <div>
            <h1>Customers</h1>
            <p>Manage your customer base and relationships</p>
        </div>
        <div class="topbar-actions">
            <button class="export-btn">
                <svg width="14" height="14" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24">
                    <path d="M21 15v4a2 2 0 01-2 2H5a2 2 0 01-2-2v-4"/>
                    <polyline points="7 10 12 15 17 10"/>
                    <line x1="12" y1="15" x2="12" y2="3"/>
                </svg>
                Export
            </button>
            <button class="add-btn" onclick="openModal()">
                <svg width="14" height="14" fill="none" stroke="currentColor" stroke-width="2.5" viewBox="0 0 24 24">
                    <circle cx="12" cy="8" r="4"/>
                    <path d="M20 21a8 8 0 10-16 0"/>
                    <line x1="19" y1="8" x2="19" y2="14"/>
                    <line x1="16" y1="11" x2="22" y2="11"/>
                </svg>
                Add Customer
            </button>
        </div>
    </div>

    <!-- ── Stat cards ── -->
    <div class="stat-grid">
        <div class="stat-card">
            <div class="stat-icon blue">
                <svg width="22" height="22" fill="none" stroke="#2563eb" stroke-width="1.8" viewBox="0 0 24 24">
                    <path d="M17 21v-2a4 4 0 00-4-4H5a4 4 0 00-4 4v2"/>
                    <circle cx="9" cy="7" r="4"/>
                    <path d="M23 21v-2a4 4 0 00-3-3.87"/><path d="M16 3.13a4 4 0 010 7.75"/>
                </svg>
            </div>
            <div>
                <div class="stat-label">Total Customers</div>
                <div class="stat-value" id="totalCustomers">2,847</div>
            </div>
        </div>
        <div class="stat-card">
            <div class="stat-icon blue">
                <svg width="22" height="22" fill="none" stroke="#2563eb" stroke-width="1.8" viewBox="0 0 24 24">
                    <path d="M16 21v-2a4 4 0 00-4-4H5a4 4 0 00-4 4v2"/>
                    <circle cx="8.5" cy="7" r="4"/>
                    <line x1="20" y1="8" x2="20" y2="14"/><line x1="23" y1="11" x2="17" y2="11"/>
                </svg>
            </div>
            <div>
                <div class="stat-label">New This Month</div>
                <div class="stat-value">156</div>
            </div>
        </div>
        <div class="stat-card">
            <div class="stat-icon green">
                <svg width="22" height="22" fill="none" stroke="#10b981" stroke-width="1.8" viewBox="0 0 24 24">
                    <path d="M17 21v-2a4 4 0 00-4-4H5a4 4 0 00-4 4v2"/>
                    <circle cx="9" cy="7" r="4"/>
                    <polyline points="16 11 18 13 22 9"/>
                </svg>
            </div>
            <div>
                <div class="stat-label">Active</div>
                <div class="stat-value">2,341</div>
            </div>
        </div>
        <div class="stat-card">
            <div class="stat-icon orange">
                <svg width="22" height="22" fill="none" stroke="#f97316" stroke-width="1.8" viewBox="0 0 24 24">
                    <polyline points="23 6 13.5 15.5 8.5 10.5 1 18"/>
                    <polyline points="17 6 23 6 23 12"/>
                </svg>
            </div>
            <div>
                <div class="stat-label">VIP Members</div>
                <div class="stat-value">89</div>
            </div>
        </div>
    </div>

    <!-- ── Filter bar ── -->
    <div class="filter-bar">
        <div class="search-wrap">
            <svg width="15" height="15" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24">
                <circle cx="11" cy="11" r="8"/><path d="M21 21l-4.35-4.35"/>
            </svg>
            <input type="text" id="searchInput" placeholder="Search customers by name or email..." oninput="filterTable()" />
        </div>

        <select class="filter-select" id="statusFilter" onchange="filterTable()">
            <option value="">All Status</option>
            <option value="Active">Active</option>
            <option value="Inactive">Inactive</option>
            <option value="VIP">VIP</option>
        </select>

        <select class="filter-select" id="sortFilter" onchange="filterTable()">
            <option value="recent">Most Recent</option>
            <option value="name">Name A–Z</option>
            <option value="spent">Highest Spent</option>
            <option value="orders">Most Orders</option>
        </select>

        <button class="more-filters-btn">
            <svg width="14" height="14" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24">
                <line x1="4" y1="6" x2="20" y2="6"/>
                <line x1="8" y1="12" x2="16" y2="12"/>
                <line x1="11" y1="18" x2="13" y2="18"/>
            </svg>
            More Filters
        </button>
    </div>

    <!-- ── Table card ── -->
    <div class="table-card">
        <div class="table-card-header">All Customers</div>
        <table>
            <thead>
                <tr>
                    <th>Customer</th>
                    <th>Phone</th>
                    <th>Orders</th>
                    <th>Total Spent</th>
                    <th>Status</th>
                    <th>Join Date</th>
                    <th>Last Order</th>
                    <th>Actions</th>
                </tr>
            </thead>
            <tbody id="customerTable">
                <!-- Rows injected by JS -->
            </tbody>
        </table>

        <!-- Pagination -->
        <div class="pagination">
            <span id="pageInfo">Showing 1–8 of 12 customers</span>
            <div class="page-btns">
                <button class="page-btn" id="prevBtn" onclick="changePage(-1)">‹</button>
                <button class="page-btn active" id="p1" onclick="goPage(1)">1</button>
                <button class="page-btn" id="p2" onclick="goPage(2)">2</button>
                <button class="page-btn" id="nextBtn" onclick="changePage(1)">›</button>
            </div>
        </div>
    </div>
</div>

<!-- ── Add Customer Modal ── -->
<div class="modal-overlay" id="modalOverlay" onclick="closeModalOutside(event)">
    <div class="modal">
        <div class="modal-header">
            <div class="modal-title">Add New Customer</div>
            <button class="modal-close" onclick="closeModal()">✕</button>
        </div>
        <div class="mrow two">
            <div class="fg"><label>First Name</label><input type="text" id="mFirstName" placeholder="John" /></div>
            <div class="fg"><label>Last Name</label><input type="text" id="mLastName" placeholder="Doe" /></div>
        </div>
        <div class="mrow one">
            <div class="fg"><label>Email Address</label><input type="email" id="mEmail" placeholder="john@example.com" /></div>
        </div>
        <div class="mrow two">
            <div class="fg"><label>Phone Number</label><input type="tel" id="mPhone" placeholder="+1 (555) 000-0000" /></div>
            <div class="fg">
                <label>Status</label>
                <select id="mStatus">
                    <option value="Active">Active</option>
                    <option value="Inactive">Inactive</option>
                    <option value="VIP">VIP</option>
                </select>
            </div>
        </div>
        <div class="modal-footer">
            <button class="cancel-btn" onclick="closeModal()">Cancel</button>
            <button class="submit-btn" onclick="addCustomer()">Add Customer</button>
        </div>
    </div>
</div>

<!-- Toast -->
<div class="toast" id="toast">Customer added successfully!</div>

<script>
    // ── Sample data ──
    const customers = [
        { id:1, first:'James',   last:'Wilson',  email:'james.wilson@email.com',    phone:'+1 (555) 123-4567', orders:12, spent:1847, status:'Active',   joined:'Jan 15, 2024', lastOrder:'Mar 23, 2026', color:'blue'   },
        { id:2, first:'Sarah',   last:'Chen',    email:'sarah.chen@email.com',      phone:'+1 (555) 234-5678', orders:8,  spent:1256, status:'Active',   joined:'Feb 22, 2024', lastOrder:'Mar 23, 2026', color:'green'  },
        { id:3, first:'Michael', last:'Brown',   email:'m.brown@email.com',         phone:'+1 (555) 345-6789', orders:5,  spent:689,  status:'Active',   joined:'Mar 10, 2024', lastOrder:'Mar 22, 2026', color:'purple' },
        { id:4, first:'Emily',   last:'Davis',   email:'emily.davis@email.com',     phone:'+1 (555) 456-7890', orders:21, spent:3240, status:'VIP',      joined:'Nov 5, 2023',  lastOrder:'Mar 21, 2026', color:'orange' },
        { id:5, first:'Daniel',  last:'Martinez',email:'d.martinez@email.com',      phone:'+1 (555) 567-8901', orders:3,  spent:312,  status:'Active',   joined:'Apr 18, 2024', lastOrder:'Mar 20, 2026', color:'teal'   },
        { id:6, first:'Olivia',  last:'Taylor',  email:'olivia.t@email.com',        phone:'+1 (555) 678-9012', orders:0,  spent:0,    status:'Inactive', joined:'May 30, 2024', lastOrder:'—',            color:'pink'   },
        { id:7, first:'William', last:'Johnson', email:'will.j@email.com',          phone:'+1 (555) 789-0123', orders:15, spent:2100, status:'VIP',      joined:'Jan 3, 2024',  lastOrder:'Mar 19, 2026', color:'blue'   },
        { id:8, first:'Ava',     last:'Anderson',email:'ava.anderson@email.com',    phone:'+1 (555) 890-1234', orders:7,  spent:945,  status:'Active',   joined:'Jun 12, 2024', lastOrder:'Mar 18, 2026', color:'green'  },
        { id:9, first:'Liam',    last:'Thomas',  email:'liam.t@email.com',          phone:'+1 (555) 901-2345', orders:2,  spent:198,  status:'Active',   joined:'Jul 22, 2024', lastOrder:'Mar 17, 2026', color:'purple' },
        { id:10,first:'Sophia',  last:'White',   email:'sophia.w@email.com',        phone:'+1 (555) 012-3456', orders:9,  spent:1430, status:'Active',   joined:'Aug 8, 2024',  lastOrder:'Mar 16, 2026', color:'orange' },
        { id:11,first:'Noah',    last:'Harris',  email:'noah.harris@email.com',     phone:'+1 (555) 123-9999', orders:4,  spent:520,  status:'Inactive', joined:'Sep 15, 2024', lastOrder:'Feb 10, 2026', color:'teal'   },
        { id:12,first:'Mia',     last:'Jackson', email:'mia.jackson@email.com',     phone:'+1 (555) 234-8888', orders:18, spent:2875, status:'VIP',      joined:'Oct 20, 2023', lastOrder:'Mar 15, 2026', color:'pink'   },
    ];

    const PER_PAGE = 8;
    let currentPage = 1;
    let filtered = [...customers];

    function initials(c) { return c.first.charAt(0) + c.last.charAt(0); }

    function badgeClass(status) {
        if (status === 'Active')   return 'badge-active';
        if (status === 'VIP')      return 'badge-vip';
        return 'badge-inactive';
    }

    function renderTable() {
        const start = (currentPage - 1) * PER_PAGE;
        const page  = filtered.slice(start, start + PER_PAGE);
        const tbody = document.getElementById('customerTable');

        tbody.innerHTML = page.map(c => `
            <tr>
                <td>
                    <div class="cust-cell">
                        <div class="av av-${c.color}">${initials(c)}</div>
                        <div>
                            <div class="cust-name">${c.first} ${c.last}</div>
                            <div class="cust-email">${c.email}</div>
                        </div>
                    </div>
                </td>
                <td class="phone-cell">${c.phone}</td>
                <td class="orders-cell">${c.orders}</td>
                <td class="spent-cell">$${c.spent.toLocaleString()}</td>
                <td><span class="badge ${badgeClass(c.status)}">${c.status}</span></td>
                <td>${c.joined}</td>
                <td>${c.lastOrder}</td>
                <td><button class="actions-btn" title="Options">···</button></td>
            </tr>
        `).join('');

        const total = filtered.length;
        const end   = Math.min(start + PER_PAGE, total);
        document.getElementById('pageInfo').textContent =
            `Showing ${start + 1}–${end} of ${total} customers`;

        const pages = Math.ceil(total / PER_PAGE);
        document.getElementById('p1').classList.toggle('active', currentPage === 1);
        document.getElementById('p2').classList.toggle('active', currentPage === 2);
        document.getElementById('p2').style.display = pages >= 2 ? '' : 'none';
        document.getElementById('prevBtn').disabled = currentPage === 1;
        document.getElementById('nextBtn').disabled = currentPage >= pages;
    }

    function filterTable() {
        const q      = document.getElementById('searchInput').value.toLowerCase();
        const status = document.getElementById('statusFilter').value;
        const sort   = document.getElementById('sortFilter').value;

        filtered = customers.filter(c => {
            const matchQ = !q || `${c.first} ${c.last} ${c.email}`.toLowerCase().includes(q);
            const matchS = !status || c.status === status;
            return matchQ && matchS;
        });

        if (sort === 'name')   filtered.sort((a,b) => a.last.localeCompare(b.last));
        if (sort === 'spent')  filtered.sort((a,b) => b.spent - a.spent);
        if (sort === 'orders') filtered.sort((a,b) => b.orders - a.orders);

        currentPage = 1;
        renderTable();
    }

    function changePage(d) { currentPage = Math.max(1, currentPage + d); renderTable(); }
    function goPage(p)     { currentPage = p; renderTable(); }

    // ── Modal ──
    function openModal()  { document.getElementById('modalOverlay').classList.add('open'); }
    function closeModal() { document.getElementById('modalOverlay').classList.remove('open'); }
    function closeModalOutside(e) { if (e.target === document.getElementById('modalOverlay')) closeModal(); }

    function addCustomer() {
        const f = document.getElementById('mFirstName').value.trim();
        const l = document.getElementById('mLastName').value.trim();
        const e = document.getElementById('mEmail').value.trim();
        const p = document.getElementById('mPhone').value.trim();
        const s = document.getElementById('mStatus').value;
        if (!f || !l || !e) return;

        const colors = ['blue','green','purple','orange','teal','pink'];
        const newC = {
            id: Date.now(), first: f, last: l, email: e,
            phone: p || '—', orders: 0, spent: 0, status: s,
            joined: new Date().toLocaleDateString('en-US',{month:'short',day:'numeric',year:'numeric'}),
            lastOrder: '—',
            color: colors[Math.floor(Math.random() * colors.length)]
        };

        customers.unshift(newC);
        document.getElementById('totalCustomers').textContent = customers.length.toLocaleString();
        filterTable();
        closeModal();

        // clear fields
        ['mFirstName','mLastName','mEmail','mPhone'].forEach(id => document.getElementById(id).value = '');

        const toast = document.getElementById('toast');
        toast.classList.add('show');
        setTimeout(() => toast.classList.remove('show'), 2600);
    }

    // Init
    renderTable();
</script>
